# TaylorSeries
A short code using Taylor Series on the function sin(x).
# Requires:-
Python 3.x,
Numpy,
Matplotlib,
# Use:-
The images will be saved in the "temp_img" folder inside the main folder.
Any other software can then be used to create a video with desired fps.
